# Fires every 20 minutes; consumed or foreign batches cannot trigger another collection run.

[Unit]
Description=lexinform: check for a finished LLM batch every 20 minutes

[Timer]
OnBootSec=5min
OnUnitActiveSec=20min
Persistent=true

[Install]
WantedBy=timers.target
