# lexinform — agent instructions

The canonical project instructions are in [AGENTS.md](AGENTS.md). Read it before changing code.

Use `README.md` for product context and `CONTRIBUTING.md` for development workflows. Read the
relevant domain guide before touching its area: `docs/legislative-process.md`,
`docs/text-selection.md`, `docs/rcl-scraping.md`, `docs/llm-cost.md`,
`docs/operator-commands.md`, or `docs/database.html`.

Legacy Claude slash commands were migrated to `.agents/skills/`; invoke the matching Codex skill.
